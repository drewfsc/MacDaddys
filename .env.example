# MongoDB Connection
# For Vercel deployment, ensure your MongoDB instance allows connections from Vercel's IP ranges
# or use MongoDB Atlas with network access set to "Allow access from anywhere" (0.0.0.0/0)
MONGODB_URI=mongodb://username:password@host:port/?authMechanism=DEFAULT
MONGODB_DB=macdaddys

# Admin Panel Password
ADMIN_PASSWORD=your-secure-admin-password

# Auth.js (NextAuth) Secret - Required for session encryption
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-random-secret-here

# Vercel Blob Storage - For menu photo uploads
# Get this from Vercel Dashboard → Storage → Create Blob Store
BLOB_READ_WRITE_TOKEN=vercel_blob_token_here
